--- a/src/modules/graphics/wrap_GraphicsShader.lua
+++ b/src/modules/graphics/wrap_GraphicsShader.lua
@@ -38,11 +38,9 @@ GLSL.SYNTAX = [[
 	#define mediump
 	#define highp
 #endif
-#if defined(VERTEX) || __VERSION__ > 100 || defined(GL_FRAGMENT_PRECISION_HIGH)
-	#define LOVE_HIGHP_OR_MEDIUMP highp
-#else
-	#define LOVE_HIGHP_OR_MEDIUMP mediump
-#endif
+// Mali 400-series: compilador aceita sintaxe highp no fragment (mesmo sendo FP16 por baixo).
+// Forçamos highp em VERTEX e FRAGMENT pra evitar mismatch no link de love_ScreenSize.
+#define LOVE_HIGHP_OR_MEDIUMP highp
 #define number float
 #define Image sampler2D
 #define ArrayImage sampler2DArray
