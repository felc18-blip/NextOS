--- a/backend/drm/legacy.c
+++ b/backend/drm/legacy.c
@@ -170,7 +170,7 @@
 	} else {
 		if (drmModeSetCursor(drm->fd, crtc->id, 0, 0, 0)) {
 			wlr_drm_conn_log_errno(conn, WLR_DEBUG, "drmModeSetCursor failed");
-			return false;
+			/* NextOS Amlogic-no: meson sem cursor plane, non-fatal */
 		}
 	}
 
