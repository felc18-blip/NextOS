# NextOS V15.10 Amlogic-no — overlay paths pra runtime SoC-aware libGL switch.
# libmali-overlay-setup script cria /var/lib/libGL.so.1 symlink baseado em SoC:
#   Valhall (s7d/s6/s5) → wrapper smart 264KB
#   Bifrost (sc2/s4)    → symlink ausente, cai no /usr/lib/libGL.so.1 (gl4es real)
#
# /var/lib first prioridade pra ld.so encontrar antes do /usr/lib.
/var/lib
/var/lib32
